# 저장소 가이드라인

## 프로젝트 구조 및 모듈 구성

이 저장소는 현재 비어 있는 프로젝트 스캐폴드입니다. 코드가 추가되면 최상위 파일은 프로젝트 설정과 문서로 제한하세요. 애플리케이션 코드는 `src/`, 자동화된 테스트는 `tests/`, 코드가 아닌 리소스는 `assets/`에 둡니다. 가능한 한 테스트 트리에서 소스 경로를 그대로 반영하세요. 예를 들어 `src/services/auth.*`에 대한 테스트는 `tests/services/` 아래에 있어야 합니다. 재사용 가능한 스크립트는 `scripts/`에 두고, 직관적이지 않은 디렉터리는 `README.md`에 설명하세요.

## 빌드, 테스트, 개발 명령

아직 빌드 시스템이나 패키지 매니저는 구성되어 있지 않습니다. 추가할 때는 작고 안정적인 명령 세트를 제공하고 README에 문서화하세요. 다음과 같은 관례적인 진입점을 권장합니다.

- `npm run dev` — 로컬 개발 환경을 시작합니다.
- `npm test` — 전체 자동화된 테스트 스위트를 실행합니다.
- `npm run lint` — 포맷과 정적 분석 규칙을 검사합니다.
- `npm run build` — 프로덕션용 산출물을 만듭니다.

생성된 빌드 산출물이나 의존성 디렉터리는 커밋하지 마세요. 실제 툴체인이 이 예시와 다르면 이 가이드를 업데이트하세요.

## 코딩 스타일 및 명명 규칙

선택한 언어의 포맷터·린터 표준을 따르고, 첫 소스 파일과 함께 해당 설정도 커밋하세요. 포맷터가 탭을 요구하지 않는 한 스페이스를 사용하세요. 이름을 명확하게 지으세요. 타입과 컴포넌트는 `PascalCase`, 함수와 변수는 `camelCase`, 일반 문서나 에셋 파일은 소문자 하이픈 표기를 사용합니다. 모듈은 하나의 역할에 집중하고, 기능 변경에 관련 없는 리팩터링은 피하세요.

## 테스트 가이드라인

동작 변경과 버그 수정마다 테스트를 추가하세요. 테스트 이름은 `rejectsExpiredToken`처럼 관찰 가능한 동작을 기준으로 지으세요. 빠른 단위 테스트는 해당 논리 모듈 근처나 대응하는 `tests/` 경로에 두고, 더 느린 통합 테스트는 명확히 분리하세요. 새 도구는 로컬과 CI에서 모든 테스트를 실행하는 명령을 하나 제공해야 합니다. 커버리지 기준이 정해지면 문서화하세요.

## 커밋 및 풀 리퀘스트 가이드라인

기존 관례를 추론할 Git 히스토리는 없습니다. 관례가 정해질 때까지는 간결한 명령형 커밋 제목을 사용하고, 선택적으로 Conventional Commits를 따르세요(예: `feat: add session validation`). 풀 리퀘스트에는 문제와 해결 방법, 수행한 검증, 관련 이슈 링크를 적으세요. 눈에 보이는 UI나 운영 변경에는 스크린샷이나 로그를 포함하세요. 각 풀 리퀘스트는 범위를 좁게 유지하고, 설정 변경이나 후속 작업은 명시하세요.

## 보안 및 설정

시크릿, 자격 증명, 로컬 환경 파일은 절대 커밋하지 마세요. `.env.example`처럼 민감 정보를 제거한 예시를 제공하고, 필요한 변수를 문서화하며, 설정에 의존하는 코드를 추가하기 전에 실제 `.env` 파일이 무시되도록 하세요.
